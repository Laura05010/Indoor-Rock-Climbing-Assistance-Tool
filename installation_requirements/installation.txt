It is recommended that you use an anaconda enviroment to run this project.
That way you will no conflict preinstalled libraries in your computer.


pip install opencv-python
pip install mediapipe
pip install numpy
pip install ultralytics
pip install supervision
pip install pynput
pip install SpeechRecognition
pip install pyaudio
pip install --upgrade supervision